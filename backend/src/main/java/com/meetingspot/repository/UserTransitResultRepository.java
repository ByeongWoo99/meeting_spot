package com.meetingspot.repository;

import com.meetingspot.entity.UserTransitResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserTransitResultRepository extends JpaRepository<UserTransitResult, Long> {
}
